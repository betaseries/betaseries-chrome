// Generated by CoffeeScript 1.7.1

/**
 * Ajax class
 * @param {object} Auth
 * @param {object} $http
 */
var Ajax;

Ajax = (function() {
  function Ajax(Auth, $http) {
    this.Auth = Auth;
    this.$http = $http;
  }


  /**
   * Performs an ajax call
   * @param  {string} method
   * @param  {string} path
   * @param  {array} params
   * @param  {callback} success
   */

  Ajax.prototype.send = function(method, path, params, success) {
    var config, defaultParams;
    defaultParams = {
      "v": '2.2',
      "key": '6db16a6ffab9',
      "token": this.Auth.getToken()
    };
    params = _.extend(params, defaultParams);
    config = {
      "method": method,
      "url": 'http://api.betaseries.com' + path,
      "params": params
    };
    return this.$http(config).success(function(data) {
      return success(data);
    }).error(function(data) {
      return console.log(data);
    });
  };


  /**
   * Shortcut ro perform a GET ajax call
   * @param  {string} path
   * @param  {array} params
   * @param  {success} success
   */

  Ajax.prototype.get = function(path, params, success) {
    return this.send('GET', path, params, success);
  };


  /**
   * Shortcut ro perform a POST ajax call
   * @param  {string} path
   * @param  {array} params
   * @param  {success} success
   */

  Ajax.prototype.post = function(path, params, success) {
    return this.send('POST', path, params, success);
  };

  return Ajax;

})();
